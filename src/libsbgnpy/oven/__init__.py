"""Work in progress.

The modules in this package are unfinished: they are not wired into the
package, not documented, not part of the public API and not shipped in the
release, see `[tool.hatch.build]` in `pyproject.toml`. They are kept in the
repository because they are the groundwork of a feature, not because they are
ready to be used.
"""
